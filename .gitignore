.claude/
/cr
dist/
codedb.snapshot
*.tasks.json.lock

# tp review and audit working files: per-role output and its merge.
# Never committed — the durable record is .tp-review/ state plus the spec itself.
/review-*.ndjson
/merged-*.ndjson
/audit-*.ndjson
