{"id": "CR-1#c1", "text": "PR identity, head SHA, merge base, and the resolved profile together with the layer that selected it per §2.4.", "source": "description", "span": "1. PR identity, head SHA, merge base, and the resolved profile together with the\n   layer that selected it per §2.4."}
{"id": "CR-1#c2", "text": "The issue key and the source it was resolved from per §3.2, with the issue text, or the reason no key was found.", "source": "description", "span": "2. The issue key and the source it was resolved from per §3.2, with the issue\n   text, or the reason no key was found."}
{"id": "CR-1#c3", "text": "The claims of §3.3, and whether the issue text has drifted per §3.3.3.", "source": "description", "span": "3. The claims of §3.3, and whether the issue text has drifted per §3.3.3."}
{"id": "CR-1#c4", "text": "The units of §3.4 with their file paths, hunk ranges, and unit hashes.", "source": "description", "span": "4. The units of §3.4 with their file paths, hunk ranges, and unit hashes."}
{"id": "CR-1#c5", "text": "The ingested threads of §3.5 and the notes of §3.6 for the issue key.", "source": "description", "span": "5. The ingested threads of §3.5 and the notes of §3.6 for the issue key."}
{"id": "CR-1#c6", "text": "The active, disabled, and unavailable axes with their reasons per §4.5.", "source": "description", "span": "6. The active, disabled, and unavailable axes with their reasons per §4.5."}
{"id": "CR-1#c7", "text": "An axis is active when it is enabled by the resolved configuration and its prerequisites are met. A **role** is active when its axis is active, its `profiles` list is empty or names the resolved profile, and §4.6.4 did not report it skipped.", "source": "description", "span": "1. An axis is active when it is enabled by the resolved configuration and its\n   prerequisites are met. A **role** is active when its axis is active, its\n   `profiles` list is empty or names the resolved profile, and §4.6.4 did not\n   report it skipped."}
{"id": "CR-1#c8", "text": "The test axis MUST be disabled automatically when the profile declares no `tests.cmd`.", "source": "description", "span": "2. The test axis MUST be disabled automatically when the profile declares no\n   `tests.cmd`."}
{"id": "CR-1#c9", "text": "The intent axis MUST be marked unavailable when no issue key resolves.", "source": "description", "span": "3. The intent axis MUST be marked unavailable when no issue key resolves."}
{"id": "CR-1#c10", "text": "Every lens that did not run MUST appear in the coverage report with its reason: a disabled axis, an unavailable axis, the unavailable reinvention half of §4.3.1, and a role skipped per §4.6.4. `cr` MUST NOT report a round as complete without stating all of them — a skipped role shrinks the cells §10", "source": "description", "span": "4. Every lens that did not run MUST appear in the coverage report with its reason:\n   a disabled axis, an unavailable axis, the unavailable reinvention half of §4.3.1,\n   and a role skipped per §4.6.4. `cr` MUST NOT report a round as complete without\n   stating all of them — a skipped role shrinks the cells §10.2.2 demands, so\n   silence about it would let a complete verdict cover a lens that never looked."}
{"id": "CR-1#c11", "text": "Every cell MUST carry the unit id and role id it sits at, one of `pass`, `finding`, `question`, or `na`, the head it was filled against, the unit hash of §3.4.6 it was filled for, and a reason when it is `na`. It MUST also carry `note_id` when a note explained an unmapped unit per §4.1.5, and `cover", "source": "description", "span": "5. Every cell MUST carry the unit id and role id it sits at, one of `pass`,\n   `finding`, `question`, or `na`, the head it was filled against, the unit hash of\n   §3.4.6 it was filled for, and a reason when it is `na`. It MUST also carry\n   `note_id` when a note explained an unmapped unit per §4.1.5, and `coverage` with\n   `test_paths` when the role is on the `test` axis per §4.4.1."}
{"id": "CR-1#c12", "text": "Cells are the agent's judgement and reach `cr` the same way findings do, through `cr cells record <pr> <file.ndjson>`, which replaces the current round's cell for each `(unit, role)` the file names and leaves every other cell untouched. `cr` MUST reject with exit code 1 a cell naming an unknown unit", "source": "description", "span": "6. Cells are the agent's judgement and reach `cr` the same way findings do, through\n   `cr cells record <pr> <file.ndjson>`, which replaces the current round's cell for\n   each `(unit, role)` the file names and leaves every other cell untouched. `cr` MUST\n   reject with exit code 1 a cell naming an unknown unit id or an inactive role,\n   and MUST NOT invent a cell for a unit no role reported on — an unfilled cell is\n   a coverage gap per §10.1.1, not something for `cr` to complete."}
{"id": "CR-1#c13", "text": "`cr sandbox create <pr>` MUST create a git worktree at the PR head under `~/.cr/state/<owner>/<repo>/pr-<n>/sandbox/`. The worktree registration under the repository's `.git/worktrees/` is the sole write permitted by §2.2.", "source": "description", "span": "1. `cr sandbox create <pr>` MUST create a git worktree at the PR head under\n   `~/.cr/state/<owner>/<repo>/pr-<n>/sandbox/`. The worktree registration under\n   the repository's `.git/worktrees/` is the sole write permitted by §2.2."}
{"id": "CR-1#c14", "text": "Paths listed in `sandbox.copy` MUST be copied from the main checkout into the sandbox after creation.", "source": "description", "span": "2. Paths listed in `sandbox.copy` MUST be copied from the main checkout into the\n   sandbox after creation."}
{"id": "CR-1#c15", "text": "Commands in `sandbox.setup` MUST run once, in order, in the sandbox root.", "source": "description", "span": "3. Commands in `sandbox.setup` MUST run once, in order, in the sandbox root."}
{"id": "CR-1#c16", "text": "`cr` MUST NOT modify the user's main worktree, index, or current branch.", "source": "description", "span": "4. `cr` MUST NOT modify the user's main worktree, index, or current branch."}
{"id": "CR-1#c17", "text": "`cr sandbox destroy <pr>` MUST remove the worktree and its registration.", "source": "description", "span": "5. `cr sandbox destroy <pr>` MUST remove the worktree and its registration."}
{"id": "CR-1#c18", "text": "Before every probe or test run, `cr` MUST verify that the sandbox HEAD equals the PR head and that the sandbox is clean. After §5.1.2 and §5.1.3 complete, `cr` MUST record the sandbox's tracked-file state as the **post-setup baseline**. **Clean** means that no tracked file differs from that baseline", "source": "description", "span": "6. Before every probe or test run, `cr` MUST verify that the sandbox HEAD equals\n   the PR head and that the sandbox is clean. After §5.1.2 and §5.1.3 complete, `cr`\n   MUST record the sandbox's tracked-file state as the **post-setup baseline**.\n   **Clean** means that no tracked file differs from that baseline, and that no\n   leftover probe artefact exists — no file under\n   any path matching `tests.probe_path_template` with its `<probe-id>` position\n   replaced by `*`, which the template MUST contain exactly once. Other\n   untracked files are ignored, because `sandbox.copy` and `sandbox.setup` create\n   them by design. A sandbox failing any of these checks MUST be recreated, and the\n   recreation MUST be reported."}
{"id": "CR-1#c19", "text": "The post-run cleanliness check runs before the probe record is written. A probe whose check fails MUST be recorded with result `error`, overriding the ladder outcome, MUST NOT grade any finding, and MUST force recreation before the next run. Such a probe establishes nothing in either direction, so §", "source": "description", "span": "7. The post-run cleanliness check runs before the probe record is written. A probe\n   whose check fails MUST be recorded with result `error`, overriding the ladder\n   outcome, MUST NOT grade any finding, and MUST force recreation before the next\n   run. Such a probe establishes nothing in either direction, so §5.3.7 MUST NOT be\n   read from it."}
{"id": "CR-1#c20", "text": "`cr test <pr> [--filter <expr>]` MUST run the profile's test command inside the sandbox and record exit code, duration, the executed and failed test counts when `tests.count_pattern` is configured, and the last `tests.output_tail_bytes` of output. `tests.count_pattern` and `tests.failed_pattern` MUS", "source": "description", "span": "1. `cr test <pr> [--filter <expr>]` MUST run the profile's test command inside the\n   sandbox and record exit code, duration, the executed and failed test counts\n   when `tests.count_pattern` is configured, and the last\n   `tests.output_tail_bytes` of output. `tests.count_pattern` and\n   `tests.failed_pattern` MUST each yield one capture group and are matched\n   **repeatedly** over the captured output: the executed count is the sum of\n   `tests.count_pattern`'s matches and the failed count the sum of\n   `tests.failed_pattern`'s. Every group MUST parse as a base-10 non-negative\n   integer; a group that does not parse leaves both counts undetermined. A\n   `tests.count_pattern` that never matches leaves both undetermined, while a\n   `tests.failed_pattern` that never matches yields a failed count of zero,\n   because a runner that prints a status only when its count is non-zero prints\n   nothing at all for no failures."}
{"id": "CR-1#c21", "text": "A baseline run with no filter MUST be recorded once per head, so pre-existing failures are never attributed to a probe. A filtered mutation probe MUST additionally record a filtered baseline for the same filter on unmutated code.", "source": "description", "span": "2. A baseline run with no filter MUST be recorded once per head, so pre-existing\n   failures are never attributed to a probe. A filtered mutation probe MUST\n   additionally record a filtered baseline for the same filter on unmutated code."}
{"id": "CR-1#c22", "text": "A run exceeding `tests.timeout_seconds` MUST be killed and recorded as `timeout`.", "source": "description", "span": "3. A run exceeding `tests.timeout_seconds` MUST be killed and recorded as\n   `timeout`."}
{"id": "CR-1#c23", "text": "Every run MUST be stored as a **run record** in `runs.ndjson` carrying `id` of the form `r<n>`, `head`, `filter` when one was given, `exit_code`, `duration_ms`, `tests_run` and `tests_failed` when derivable, `output_tail`, `passed`, and `probe` when the run was performed on mutated or probe-injected", "source": "description", "span": "4. Every run MUST be stored as a **run record** in `runs.ndjson` carrying `id` of\n   the form `r<n>`, `head`, `filter` when one was given, `exit_code`,\n   `duration_ms`, `tests_run` and `tests_failed` when derivable, `output_tail`,\n   `passed`, and `probe` when the run was performed on mutated or probe-injected\n   code. This id\n   space is what a probe's `baseline` field references."}
{"id": "CR-1#c24", "text": "A run record has `passed: true` when `exit_code` is 0, `tests_run` is derivable and greater than 0, and `tests_failed` is 0; otherwise `passed: false`. A run whose counts are underivable never passes, so it cannot serve as a baseline.", "source": "description", "span": "5. A run record has `passed: true` when `exit_code` is 0, `tests_run` is derivable\n   and greater than 0, and `tests_failed` is 0; otherwise `passed: false`. A run\n   whose counts are underivable never passes, so it cannot serve as a baseline."}
{"id": "CR-1#c25", "text": "`cr probe run` MUST resolve its baseline per §5.5 and, when no matching run exists at the current head, MUST perform and record it before the probe. When more than one matches, the most recent MUST be used. Only a run record carrying no `probe` may serve as a baseline.", "source": "description", "span": "6. `cr probe run` MUST resolve its baseline per §5.5 and, when no matching run\n   exists at the current head, MUST perform and record it before the probe. When\n   more than one matches, the most recent MUST be used. Only a run record carrying\n   no `probe` may serve as a baseline."}
{"id": "CR-1#c26", "text": "The agent supplies a mutation as a unified diff against a sandbox file.", "source": "description", "span": "1. The agent supplies a mutation as a unified diff against a sandbox file."}
{"id": "CR-1#c27", "text": "`cr probe run <pr> --kind mutation --patch <file> --filter <expr>` MUST apply the mutation, run the tests, revert the mutation, and record the result. `cr` MUST derive `target` from the patch itself — the path and pre-image line of the first hunk's first removed or replaced line, or when that hunk o", "source": "description", "span": "2. `cr probe run <pr> --kind mutation --patch <file> --filter <expr>` MUST apply the\n   mutation, run the tests, revert the mutation, and record the result. `cr` MUST\n   derive `target` from the patch itself — the path and pre-image line of the first\n   hunk's first removed or replaced line, or when that hunk only adds lines, its\n   first pre-image line — and\n   MUST reject `--target` for this kind. The evidence chain from experiment to\n   assertion then runs on the patch `cr` executed, not on a flag the agent typed."}
{"id": "CR-1#c28", "text": "The mutation MUST be reverted even when the run fails, times out, or `cr` is killed. On the next invocation the check of §5.1.6 MUST detect an unclean sandbox and recreate it before running anything.", "source": "description", "span": "3. The mutation MUST be reverted even when the run fails, times out, or `cr` is\n   killed. On the next invocation the check of §5.1.6 MUST detect an unclean\n   sandbox and recreate it before running anything."}
{"id": "CR-1#c29", "text": "The result MUST be determined by the first matching rung of this ladder, which is total over every run — no run can match two rungs, and none can match none: 1. the mutation patch did not apply cleanly → `error`, and no tests are run; 2. the run was killed for exceeding `tests.timeout_seconds` → `ti", "source": "description", "span": "4. The result MUST be determined by the first matching rung of this ladder, which\n   is total over every run — no run can match two rungs, and none can match none:\n   1. the mutation patch did not apply cleanly → `error`, and no tests are run;\n   2. the run was killed for exceeding `tests.timeout_seconds` → `timeout`;\n   3. the runner could not be started, or exited on a signal → `error`;\n   4. the executed test count is known and equals zero → `no-tests-selected`;\n   5. the executed or failed count is undetermined, whether because no\n      `tests.count_pattern` is configured or because the configured pattern did\n      not match the output → `inconclusive`;\n   6. the failed count is zero → `no-test-failed`;\n   7. otherwise → `failed`."}
{"id": "CR-1#c30", "text": "Only `no-test-failed` proves the gap, and only when the probe's `baseline` run record has `passed: true` per §5.2.5. `timeout`, `error`, `no-tests-selected`, and `inconclusive` MUST NOT support a `probed` grade, so an empty selection, an unparseable runner, and a run that never finished cannot manuf", "source": "description", "span": "5. Only `no-test-failed` proves the gap, and only when the probe's `baseline` run\n   record has `passed: true` per §5.2.5. `timeout`, `error`, `no-tests-selected`,\n   and `inconclusive` MUST NOT support a `probed` grade, so an empty selection, an\n   unparseable runner, and a run that never finished cannot manufacture evidence."}
{"id": "CR-1#c31", "text": "A filtered run proves the gap **only for the tests it selected**. `cr` cannot establish that a filter selects the tests which would have caught the mutation, so it MUST NOT claim it: the filter MUST be carried into the §8.1.7 evidence region, and §5.3.5's guarantee MUST NOT be stated more widely tha", "source": "description", "span": "6. A filtered run proves the gap **only for the tests it selected**. `cr` cannot\n   establish that a filter selects the tests which would have caught the mutation,\n   so it MUST NOT claim it: the filter MUST be carried into the §8.1.7 evidence\n   region, and §5.3.5's guarantee MUST NOT be stated more widely than the empty\n   selection it actually catches."}
{"id": "CR-1#c32", "text": "A result of `failed` disproves the gap; `cr` MUST record it and the agent MUST NOT raise the finding.", "source": "description", "span": "7. A result of `failed` disproves the gap; `cr` MUST record it and the agent MUST\n   NOT raise the finding."}
{"id": "CR-1#c33", "text": "The agent supplies a new test file targeting the suspected edge case.", "source": "description", "span": "1. The agent supplies a new test file targeting the suspected edge case."}
{"id": "CR-1#c34", "text": "`cr probe run <pr> --kind gap --test <file> --target <path:line> --filter <expr>` MUST place the test in the sandbox at the path given by the profile's `tests.probe_path_template` (§2.4), run it, remove it, and record the result. The template is fixed per profile so §5.1.6 can recognise a leftover a", "source": "description", "span": "2. `cr probe run <pr> --kind gap --test <file> --target <path:line> --filter <expr>`\n   MUST place the test in the sandbox at the path given by the profile's\n   `tests.probe_path_template` (§2.4), run it, remove it, and record the result.\n   The template is fixed per profile so §5.1.6 can recognise a leftover artefact\n   and the runner's own discovery still finds the file. An existing file at that path MUST abort with exit code 4. The removal\n   MUST happen even when the run fails or times out, and §5.1.6 catches the case\n   where it did not."}
{"id": "CR-1#c35", "text": "The result MUST be determined by the first matching rung of this ladder, which is total over every run: 1. the run was killed for exceeding `tests.timeout_seconds` → `timeout`; 2. the runner could not be started → `error`; 3. the executed test count is known and equals zero, so the filter excluded t", "source": "description", "span": "3. The result MUST be determined by the first matching rung of this ladder, which\n   is total over every run:\n   1. the run was killed for exceeding `tests.timeout_seconds` → `timeout`;\n   2. the runner could not be started → `error`;\n   3. the executed test count is known and equals zero, so the filter excluded the\n      supplied test or it failed to load → `no-tests-selected`;\n   4. the executed or failed count is undetermined, whether because no\n      `tests.count_pattern` is configured or because the configured pattern did\n      not match → `inconclusive`;\n   5. the failed count is zero → `passed`;\n   6. otherwise → `failed`.\n   Rung 4 exists for the same reason as §5.3.4's: without it a profile that cannot\n   report counts would drive every gap probe to `failed`, and §5.4.4 would raise it\n   at severity `high` on no evidence at all."}
{"id": "CR-1#c36", "text": "A `failed` gap probe means either the behaviour is wrong or the supplied test is wrong, and `cr` cannot distinguish the two. The result MUST be recorded and the probe output offered as a reproduction. The probe MUST be treated as supporting the finding only when its baseline passed per §5.2.5 **and*", "source": "description", "span": "4. A `failed` gap probe means either the behaviour is wrong or the supplied test is\n   wrong, and `cr` cannot distinguish the two. The result MUST be recorded and the\n   probe output offered as a reproduction. The probe MUST be treated as supporting\n   the finding only when its baseline passed per §5.2.5 **and** the finding's\n   `claim` field names a claim mapped to the finding's unit; otherwise it MUST NOT support a\n   `probed` grade, which leaves the record `argued` under §6.2 and therefore a\n   question under §6.3. Severity MUST be at least `high` only when the probe\n   supports the finding."}
{"id": "CR-1#c37", "text": "A `passed` gap probe establishes that the behaviour is present, not that the suite lacks a test — only §5.3's `no-test-failed` establishes that. It MUST NOT support a `probed` grade, and neither may `timeout`, `error`, `no-tests-selected`, or `inconclusive`; a record resting on one falls to `argued`", "source": "description", "span": "5. A `passed` gap probe establishes that the behaviour is present, not that the\n   suite lacks a test — only §5.3's `no-test-failed` establishes that. It MUST NOT\n   support a `probed` grade, and neither may `timeout`, `error`,\n   `no-tests-selected`, or `inconclusive`; a record resting on one falls to `argued`\n   under §6.2 and is posted as a question. Severity MUST be at most `medium`."}
{"id": "CR-1#c38", "text": "5.5 Probe record: `id` | yes | `p<n>`", "source": "description", "span": "| `id` | yes | `p<n>` |"}
{"id": "CR-1#c39", "text": "5.5 Probe record: `kind` | yes | `mutation` or `gap`", "source": "description", "span": "| `kind` | yes | `mutation` or `gap` |"}
{"id": "CR-1#c40", "text": "5.5 Probe record: `head` | yes | Head SHA the probe ran against", "source": "description", "span": "| `head` | yes | Head SHA the probe ran against |"}
{"id": "CR-1#c41", "text": "5.5 Probe record: `input` | yes | The patch or test file content", "source": "description", "span": "| `input` | yes | The patch or test file content |"}
{"id": "CR-1#c42", "text": "5.5 Probe record: `filter` | no | The test filter used", "source": "description", "span": "| `filter` | no | The test filter used |"}
{"id": "CR-1#c43", "text": "5.5 Probe record: `result` | yes | A value from the per-kind table below", "source": "description", "span": "| `result` | yes | A value from the per-kind table below |"}
{"id": "CR-1#c44", "text": "5.5 Probe record: `tests_run` | no | Executed test count when derivable", "source": "description", "span": "| `tests_run` | no | Executed test count when derivable |"}
{"id": "CR-1#c45", "text": "5.5 Probe record: `tests_failed` | no | Failed test count when derivable", "source": "description", "span": "| `tests_failed` | no | Failed test count when derivable |"}
{"id": "CR-1#c46", "text": "5.5 Probe record: `baseline` | yes | Id of the run record (§5.2.4) at the same head measuring the same tests on unmutated, un-probed code: for `mutation`, the run with this probe's filter; for `gap`, the unfiltered run, since the probe's own test does not exist in the baseline", "source": "description", "span": "| `baseline` | yes | Id of the run record (§5.2.4) at the same head measuring the same tests on unmutated, un-probed code: for `mutation`, the run with this probe's filter; for `gap`, the unfiltered run, since the probe's own test does not exist in the baseline |"}
{"id": "CR-1#c47", "text": "5.5 Probe record: `target` | computed for `mutation`, `--target` for `gap` | `path:line` the probe addresses, always `side: RIGHT` since probes run against the sandbox at head; for `mutation` derived from the patch per §5.3.2, for `gap` supplied and validated as §6.2.3 validates a citation", "source": "description", "span": "| `target` | computed for `mutation`, `--target` for `gap` | `path:line` the probe addresses, always `side: RIGHT` since probes run against the sandbox at head; for `mutation` derived from the patch per §5.3.2, for `gap` supplied and validated as §6.2.3 validates a citation |"}
{"id": "CR-1#c48", "text": "5.5 Probe record: `duration_ms` | yes | Wall-clock duration", "source": "description", "span": "| `duration_ms` | yes | Wall-clock duration |"}
{"id": "CR-1#c49", "text": "5.5 Probe record: `output_tail` | yes | Runner output truncated to `tests.output_tail_bytes`", "source": "description", "span": "| `output_tail` | yes | Runner output truncated to `tests.output_tail_bytes` |"}
{"id": "CR-1#c50", "text": "5.5 Probe record: `mutation` | `no-test-failed`, `failed`, `no-tests-selected`, `inconclusive`, `error`, `timeout`", "source": "description", "span": "| `mutation` | `no-test-failed`, `failed`, `no-tests-selected`, `inconclusive`, `error`, `timeout` |"}
{"id": "CR-1#c51", "text": "5.5 Probe record: `gap` | `passed`, `failed`, `no-tests-selected`, `inconclusive`, `error`, `timeout`", "source": "description", "span": "| `gap` | `passed`, `failed`, `no-tests-selected`, `inconclusive`, `error`, `timeout` |"}
{"id": "CR-1#c52", "text": "Probe records MUST be immutable once written.", "source": "description", "span": "1. Probe records MUST be immutable once written."}
{"id": "CR-1#c53", "text": "A finding MUST reference at most one probe, by id.", "source": "description", "span": "2. A finding MUST reference at most one probe, by id."}
{"id": "CR-1#c54", "text": "Probe records whose head differs from the current head MUST NOT be used to grade a finding in the current round.", "source": "description", "span": "3. Probe records whose head differs from the current head MUST NOT be used to grade\n   a finding in the current round."}
{"id": "CR-1#c55", "text": "Probe and test runs MUST take an advisory lock named after the absolute path of the repository under review together with the profile id, so two `cr` runs never share a test database and two unrelated repositories never block each other.", "source": "description", "span": "1. Probe and test runs MUST take an advisory lock named after the absolute path of\n   the repository under review together with the profile id, so two `cr` runs never\n   share a test database and two unrelated repositories never block each other."}
{"id": "CR-1#c56", "text": "When the lock is held, `cr` MUST wait up to `probe.lock_timeout_seconds` (default 300) and then fail with exit code 4.", "source": "description", "span": "2. When the lock is held, `cr` MUST wait up to `probe.lock_timeout_seconds`\n   (default 300) and then fail with exit code 4."}
{"id": "CR-1#c57", "text": "`cr` MUST warn that an unrelated local test run can still collide, because the lock only covers `cr`'s own runs.", "source": "description", "span": "3. `cr` MUST warn that an unrelated local test run can still collide, because the\n   lock only covers `cr`'s own runs."}
{"id": "CR-1#c58", "text": "`probe.max_per_round` (default 10) MUST cap probe executions per round; the cap being hit MUST be reported, never silently applied.", "source": "description", "span": "4. `probe.max_per_round` (default 10) MUST cap probe executions per round; the cap\n   being hit MUST be reported, never silently applied."}
{"id": "CR-1#c59", "text": "Each record MUST be rendered as a block introduced by an HTML comment marker carrying `id`, `kind`, `path`, `start_line`, `line`, `severity`, `grade`, and `disposition`.", "source": "description", "span": "1. Each record MUST be rendered as a block introduced by an HTML comment marker\n   carrying `id`, `kind`, `path`, `start_line`, `line`, `severity`, `grade`, and\n   `disposition`."}
{"id": "CR-1#c60", "text": "The block body MUST be free-form Markdown that the user may rewrite entirely.", "source": "description", "span": "2. The block body MUST be free-form Markdown that the user may rewrite entirely."}
{"id": "CR-1#c61", "text": "A suggestion MUST be rendered as a fenced `suggestion` block inside the body, and a suggestion with `suggestion_origin: rule` MUST be labelled as machine generated.", "source": "description", "span": "3. A suggestion MUST be rendered as a fenced `suggestion` block inside the body,\n   and a suggestion with `suggestion_origin: rule` MUST be labelled as machine\n   generated."}
{"id": "CR-1#c62", "text": "The file MUST open with a summary header listing counts, the coverage state, and the comment count against `post.max_comments`, as comments that are not posted.", "source": "description", "span": "4. The file MUST open with a summary header listing counts, the coverage state,\n   and the comment count against `post.max_comments`, as comments that are not\n   posted."}
{"id": "CR-1#c63", "text": "`cr` MUST write each record's agent region exactly as rendered to `rounds/<n>/rendered.json`. `cr`-owned regions are excluded, since `cr` regenerates them. `rendered.json` MUST hold only the body `cr` itself generated; a body preserved per §7.1.6 MUST NOT replace its entry.", "source": "description", "span": "5. `cr` MUST write each record's agent region exactly as rendered to\n   `rounds/<n>/rendered.json`. `cr`-owned regions are excluded, since `cr`\n   regenerates them. `rendered.json` MUST hold only the body `cr` itself\n   generated; a body preserved per §7.1.6 MUST NOT replace its entry."}
{"id": "CR-1#c64", "text": "The draft MUST be regenerable. Regeneration MUST first ingest the current draft's triage state per §7.2 — including deletions, which become discards and waivers at that moment — and MUST then preserve bodies for records whose body differs from `rendered.json`. Regeneration MUST NOT resurrect a delet", "source": "description", "span": "6. The draft MUST be regenerable. Regeneration MUST first ingest the current\n   draft's triage state per §7.2 — including deletions, which become discards and\n   waivers at that moment — and MUST then preserve bodies for records whose body\n   differs from `rendered.json`. Regeneration MUST NOT resurrect a deleted block."}
{"id": "CR-1#c65", "text": "7.2 Triage verbs: User action in `draft.md` | Effect", "source": "description", "span": "| User action in `draft.md` | Effect |"}
{"id": "CR-1#c66", "text": "7.2 Triage verbs: Leaves a block unchanged | Posted as rendered", "source": "description", "span": "| Leaves a block unchanged | Posted as rendered |"}
{"id": "CR-1#c67", "text": "7.2 Triage verbs: Edits the body prose | Posted as edited", "source": "description", "span": "| Edits the body prose | Posted as edited |"}
{"id": "CR-1#c68", "text": "7.2 Triage verbs: Changes `kind=finding` to `kind=question` | Softened, and recorded as a triage event", "source": "description", "span": "| Changes `kind=finding` to `kind=question` | Softened, and recorded as a triage event |"}
{"id": "CR-1#c69", "text": "7.2 Triage verbs: Deletes the block entirely | Discarded with disposition `not-here`; a pull-request-scoped waiver is written per §7.4", "source": "description", "span": "| Deletes the block entirely | Discarded with disposition `not-here`; a pull-request-scoped waiver is written per §7.4 |"}
{"id": "CR-1#c70", "text": "7.2 Triage verbs: Sets `disposition=wrong` in the marker | Discarded as a false positive whether or not the body remains; a repository-wide waiver is written per §7.4 and it counts against the class per §7.3", "source": "description", "span": "| Sets `disposition=wrong` in the marker | Discarded as a false positive whether or not the body remains; a repository-wide waiver is written per §7.4 and it counts against the class per §7.3 |"}
{"id": "CR-1#c71", "text": "7.2 Triage verbs: `id` | Immutable; a changed or unknown id aborts", "source": "description", "span": "| `id` | Immutable; a changed or unknown id aborts |"}
{"id": "CR-1#c72", "text": "7.2 Triage verbs: `kind` | `finding`→`question` softens; `question`→`finding` is accepted only when the recomputed grade is `probed` or `cited`, and aborts otherwise per §6.3.3", "source": "description", "span": "| `kind` | `finding`→`question` softens; `question`→`finding` is accepted only when the recomputed grade is `probed` or `cited`, and aborts otherwise per §6.3.3 |"}
{"id": "CR-1#c73", "text": "7.2 Triage verbs: `path`, `start_line`, `line` | Re-validated per §6.1.2; aborts when the anchor no longer resolves", "source": "description", "span": "| `path`, `start_line`, `line` | Re-validated per §6.1.2; aborts when the anchor no longer resolves |"}
{"id": "CR-1#c74", "text": "7.2 Triage verbs: `severity` | Freely editable; recorded as a triage event", "source": "description", "span": "| `severity` | Freely editable; recorded as a triage event |"}
{"id": "CR-1#c75", "text": "7.2 Triage verbs: `disposition` | Setting it to `wrong` discards the record as a false positive; `not-here` is written by `cr` when a block is deleted and MUST NOT be set by hand", "source": "description", "span": "| `disposition` | Setting it to `wrong` discards the record as a false positive; `not-here` is written by `cr` when a block is deleted and MUST NOT be set by hand |"}
{"id": "CR-1#c76", "text": "7.2 Triage verbs: `grade` | Informational. `cr` recomputes it per §6.2 and ignores whatever the marker holds; it aborts only when the marker's `kind` contradicts the recomputed grade per §6.3.3", "source": "description", "span": "| `grade` | Informational. `cr` recomputes it per §6.2 and ignores whatever the marker holds; it aborts only when the marker's `kind` contradicts the recomputed grade per §6.3.3 |"}
{"id": "CR-1#c77", "text": "`cr post` MUST refuse to run when a marker is malformed, naming the line.", "source": "description", "span": "1. `cr post` MUST refuse to run when a marker is malformed, naming the line."}
{"id": "CR-1#c78", "text": "`cr post` MUST recompute every record's grade and re-apply §6.3 before building the payload, so no draft edit can turn an `argued` record into a posted assertion.", "source": "description", "span": "2. `cr post` MUST recompute every record's grade and re-apply §6.3 before building\n   the payload, so no draft edit can turn an `argued` record into a posted\n   assertion."}
{"id": "CR-1#c79", "text": "v0.1 has no manual-comment channel in the draft. A block whose `id` is unknown MUST abort with exit code 1 rather than being adopted as a new record: every record in the corpus carries a role, an axis, and a grade `cr` computed, and a hand-written block can carry none of them. A comment the reviewer", "source": "description", "span": "3. v0.1 has no manual-comment channel in the draft. A block whose `id` is unknown\n   MUST abort with exit code 1 rather than being adopted as a new record: every\n   record in the corpus carries a role, an axis, and a grade `cr` computed, and a\n   hand-written block can carry none of them. A comment the reviewer wants to make\n   in their own name is written on GitHub directly, after posting."}
{"id": "CR-1#c80", "text": "Triage events MUST be keyed by `(PR, round, record id, action)` and written idempotently: re-running a command MUST overwrite the event for that key rather than append a second one, so regenerating a draft per §7.1.6 or running `cr post` twice cannot inflate a count. `cr draft` MUST write one `raise", "source": "description", "span": "1. Triage events MUST be keyed by `(PR, round, record id, action)` and written\n   idempotently: re-running a command MUST overwrite the event for that key rather\n   than append a second one, so regenerating a draft per §7.1.6 or running\n   `cr post` twice cannot inflate a count. `cr draft` MUST write one `raised` event\n   per record it queues, and MUST write the outcome event directly for a record it\n   discards at draft time. `cr post --confirm` MUST write exactly one outcome event\n   per queued record, drawn from `kept`, `softened`, `discarded-not-here`, and\n   `discarded-wrong`; `cr post` without `--confirm` MUST write none, since it\n   changes nothing. Every event MUST carry class, axis, role, grade, rule id when\n   present, the PR, the round, and the head. These five action names are the\n   complete vocabulary the statistics below are computed from."}
{"id": "CR-1#c81", "text": "`cr stats --repo <owner/repo>` MUST report per class and per rule: raised, kept, softened, discarded as `not-here`, and discarded as `wrong`.", "source": "description", "span": "2. `cr stats --repo <owner/repo>` MUST report per class and per rule: raised, kept,\n   softened, discarded as `not-here`, and discarded as `wrong`."}
{"id": "CR-1#c82", "text": "`class` is agent-composed, so a reworded slug would silently escape the waiver and posted-index keys of §7.4.1 and §9.3.6. `cr` MUST therefore report every class first seen in a round as new, in the round summary and in `cr stats`, so drift is visible rather than silent.", "source": "description", "span": "3. `class` is agent-composed, so a reworded slug would silently escape the waiver\n   and posted-index keys of §7.4.1 and §9.3.6. `cr` MUST therefore report every\n   class first seen in a round as new, in the round summary and in `cr stats`, so\n   drift is visible rather than silent."}
{"id": "CR-1#c83", "text": "The demotion rate for a class MUST be computed as `(discarded-wrong + softened) / raised` over that class's events in the repository's `triage.ndjson`. Discards dispositioned `not-here` MUST be excluded from the numerator, because they carry no evidence that the class is imprecise. A class whose rat", "source": "description", "span": "4. The demotion rate for a class MUST be computed as\n   `(discarded-wrong + softened) / raised` over that class's events in the\n   repository's `triage.ndjson`. Discards dispositioned `not-here` MUST be excluded\n   from the numerator, because they carry no evidence that the class is imprecise.\n   A class whose rate exceeds `stats.demote_threshold` (default 0.6) over at least\n   `stats.min_samples` (default 8) raised events MUST be listed as a demotion\n   candidate."}
{"id": "CR-1#c84", "text": "Marking a false positive `wrong` costs the reviewer an extra edit while plain deletion is free, so `discarded-wrong` is systematically under-counted and the demotion rate is a lower bound on imprecision. `cr` MUST report it as a lower bound and MUST NOT present it as a measured precision.", "source": "description", "span": "5. Marking a false positive `wrong` costs the reviewer an extra edit while plain\n   deletion is free, so `discarded-wrong` is systematically under-counted and the\n   demotion rate is a lower bound on imprecision. `cr` MUST report it as a lower\n   bound and MUST NOT present it as a measured precision."}
{"id": "CR-1#c85", "text": "A class whose `not-here` rate exceeds `stats.demote_threshold` over the same sample MUST be reported separately as a **volume candidate**: it is accurate but rarely worth posting, and the remedy is to stop raising it, not to soften it.", "source": "description", "span": "6. A class whose `not-here` rate exceeds `stats.demote_threshold` over the same\n   sample MUST be reported separately as a **volume candidate**: it is accurate but\n   rarely worth posting, and the remedy is to stop raising it, not to soften it."}
{"id": "CR-1#c86", "text": "Both candidacies are reports to the user, not automatic changes. `cr` MUST NOT alter a rule's `kind` by itself.", "source": "description", "span": "7. Both candidacies are reports to the user, not automatic changes. `cr` MUST NOT\n   alter a rule's `kind` by itself."}
{"id": "CR-1#c87", "text": "A waiver MUST be keyed by `(anchor.path, class, normalised hash of the anchored lines)`, matching the class-based identity of §6.4.1 so a waived finding cannot return under a different producer. Its scope follows its disposition: a waiver written for `wrong` is scoped to the repository, and one writ", "source": "description", "span": "1. A waiver MUST be keyed by `(anchor.path, class, normalised hash of the anchored\n   lines)`, matching the class-based identity of §6.4.1 so a waived finding cannot\n   return under a different producer. Its scope follows its disposition: a waiver\n   written for `wrong` is scoped to the repository, and one written for `not-here`\n   is scoped to the pull request. The summary is deliberately excluded from the\n   key: it is agent-composed prose that differs between rounds, and including it\n   would let a waived finding resurface under a reworded summary."}
{"id": "CR-1#c88", "text": "The key is narrow on purpose. It suppresses the same class at the same unchanged code in the same file, and stops suppressing once that code changes — which is exactly when the judgement behind the waiver should be revisited.", "source": "description", "span": "2. The key is narrow on purpose. It suppresses the same class at the same unchanged\n   code in the same file, and stops suppressing once that code changes — which is\n   exactly when the judgement behind the waiver should be revisited."}
{"id": "CR-1#c89", "text": "The two scopes follow the two meanings. \"This is wrong\" is a fact about the class and generalises across the repository; \"not worth saying here\" is a fact about this pull request and MUST NOT silence the finding anywhere else.", "source": "description", "span": "3. The two scopes follow the two meanings. \"This is wrong\" is a fact about the\n   class and generalises across the repository; \"not worth saying here\" is a fact\n   about this pull request and MUST NOT silence the finding anywhere else."}
{"id": "CR-1#c90", "text": "The two scopes live in two files. A repository-wide waiver is written to `~/.cr/waivers/<owner>/<repo>.ndjson`; a pull-request-scoped one is written to that PR's `waivers.ndjson` per §2.3. Both MUST be readable and removable, so neither disposition is a one-way door.", "source": "description", "span": "4. The two scopes live in two files. A repository-wide waiver is written to\n   `~/.cr/waivers/<owner>/<repo>.ndjson`; a pull-request-scoped one is written to\n   that PR's `waivers.ndjson` per §2.3. Both MUST be readable and removable, so\n   neither disposition is a one-way door."}
{"id": "CR-1#c91", "text": "A waiver MUST record its `disposition` per §7.2, so §7.3 can tell a false positive from a deliberate silence.", "source": "description", "span": "5. A waiver MUST record its `disposition` per §7.2, so §7.3 can tell a false\n   positive from a deliberate silence."}
{"id": "CR-1#c92", "text": "Waived findings MUST be dropped before drafting and counted in the round summary.", "source": "description", "span": "6. Waived findings MUST be dropped before drafting and counted in the round summary."}
{"id": "CR-1#c93", "text": "`cr waivers list --repo <owner/repo> [--pr <number>]` MUST print active waivers with their scope and disposition, covering both files of §7.4.4 when `--pr` is given and the repository-wide file alone when it is not. `cr waivers remove <id> --repo <owner/repo> [--pr <number>]` MUST delete one from ei", "source": "description", "span": "7. `cr waivers list --repo <owner/repo> [--pr <number>]` MUST print active waivers\n   with their scope and disposition, covering both files of §7.4.4 when `--pr` is\n   given and the repository-wide file alone when it is not.\n   `cr waivers remove <id> --repo <owner/repo> [--pr <number>]` MUST delete one\n   from either scope."}
{"id": "CR-1#c94", "text": "A waiver MUST record the round, the PR, the head, and the reason when one was given.", "source": "description", "span": "8. A waiver MUST record the round, the PR, the head, and the reason when one was\n   given."}
{"id": "CR-1#c95", "text": "`cr` MUST compare the current head to `meta.json`'s recorded head on every command that reads per-PR state, and MUST report both when they differ.", "source": "description", "span": "1. `cr` MUST compare the current head to `meta.json`'s recorded head on every\n   command that reads per-PR state, and MUST report both when they differ."}
{"id": "CR-1#c96", "text": "When they differ, every command that writes per-PR state MUST refuse with exit code 4, naming both heads; `cr brief` is the only way forward. `cr post --reconcile` is exempt: it anchors nothing and only matches the §8.4.3 payload hash against the PR's existing reviews.", "source": "description", "span": "2. When they differ, every command that writes per-PR state MUST refuse with exit\n   code 4, naming both heads; `cr brief` is the only way forward. `cr post\n   --reconcile` is exempt: it anchors nothing and only matches the §8.4.3 payload\n   hash against the PR's existing reviews."}
{"id": "CR-1#c97", "text": "`cr brief` MUST increment `meta.json`'s round index if and only if the current head differs from the recorded head, and MUST then store the new head. A same-head `cr brief` MUST be idempotent.", "source": "description", "span": "3. `cr brief` MUST increment `meta.json`'s round index if and only if the current\n   head differs from the recorded head, and MUST then store the new head. A\n   same-head `cr brief` MUST be idempotent."}
{"id": "CR-1#c98", "text": "On that increment every record still in `draft` or `queued` MUST move to `stale`, `mapping.ndjson` MUST be cleared, and `cr brief` MUST recompute units and threads for the new head. Claims are carried forward unchanged; `cr brief` MUST re-read the issue text and re-run the §3.3.3 drift comparison, a", "source": "description", "span": "4. On that increment every record still in `draft` or `queued` MUST move to\n   `stale`, `mapping.ndjson` MUST be cleared, and `cr brief` MUST recompute units\n   and threads for the new head. Claims are carried forward unchanged; `cr brief`\n   MUST re-read the issue text and re-run the §3.3.3 drift comparison, and MUST NOT\n   re-extract."}
{"id": "CR-1#c99", "text": "Every record carries `round` and `head`, and a command MUST read only the current round's records; earlier rounds are history. A command documented as replacing or clearing a file does so for the current round only and MUST leave earlier rounds intact. Waivers per §7.4, the context store per §3.6, a", "source": "description", "span": "5. Every record carries `round` and `head`, and a command MUST read only the\n   current round's records; earlier rounds are history. A command documented as\n   replacing or clearing a file does so for the current round only and MUST leave\n   earlier rounds intact. Waivers per §7.4, the context store per §3.6, and the\n   posted index of §9.3.6 apply across rounds and are exempt from this scoping."}
{"id": "CR-1#c100", "text": "`cr` MUST maintain a **posted index** in `posted-index.ndjson`, keyed exactly as a §7.4.1 waiver is, holding one entry per record that reached `posted`. A finding matching an entry MUST be dropped at merge and counted in the round summary, and MUST NOT be written to `findings.ndjson` at all, exactly", "source": "description", "span": "6. `cr` MUST maintain a **posted index** in `posted-index.ndjson`, keyed exactly as\n   a §7.4.1 waiver is, holding one entry per record that reached `posted`. A\n   finding matching an entry MUST be dropped at merge and counted in the round\n   summary, and MUST NOT be written to `findings.ndjson` at all, exactly as §6.4.4\n   drops a waived finding."}
{"id": "CR-1#c101", "text": "Units total, units with a complete row of cells, units with gaps, and units flagged `oversized` per §3.4.5.", "source": "description", "span": "1. Units total, units with a complete row of cells, units with gaps, and units\n   flagged `oversized` per §3.4.5."}
{"id": "CR-1#c102", "text": "Claims total, claims mapped to units, and the unimplemented-claim entries of §4.1.3, which appear here and nowhere else because they are never posted.", "source": "description", "span": "2. Claims total, claims mapped to units, and the unimplemented-claim entries of\n   §4.1.3, which appear here and nowhere else because they are never posted."}
{"id": "CR-1#c103", "text": "Active axes, and every lens of §4.5.4 that did not run, with its reason.", "source": "description", "span": "3. Active axes, and every lens of §4.5.4 that did not run, with its reason."}
{"id": "CR-1#c104", "text": "Findings and questions by state, severity, and grade.", "source": "description", "span": "4. Findings and questions by state, severity, and grade."}
{"id": "CR-1#c105", "text": "Probes run, and probes that changed a finding's grade.", "source": "description", "span": "5. Probes run, and probes that changed a finding's grade."}
{"id": "CR-1#c106", "text": "Waivers applied and duplicates suppressed in the current round.", "source": "description", "span": "6. Waivers applied and duplicates suppressed in the current round."}
{"id": "CR-1#c107", "text": "The current head equals the head the round was recorded against, per §9.3.1.", "source": "description", "span": "1. The current head equals the head the round was recorded against, per §9.3.1."}
{"id": "CR-1#c108", "text": "Every unit has a complete row of cells for every active role, and every one of those cells was filled for that unit's current unit hash per §3.4.6.", "source": "description", "span": "2. Every unit has a complete row of cells for every active role, and every one of\n   those cells was filled for that unit's current unit hash per §3.4.6."}
{"id": "CR-1#c109", "text": "Every claim is mapped to at least one unit. A claim carrying an unimplemented-claim entry per §4.1.3 MUST block completeness until it is either mapped, or set aside through `cr claims set-aside` per §4.1.8. The entry is not a record, so §10.2.4 never reaches it; this condition is the only thing that", "source": "description", "span": "3. Every claim is mapped to at least one unit. A claim carrying an\n   unimplemented-claim entry per §4.1.3 MUST block completeness until it is either\n   mapped, or set aside through `cr claims set-aside` per §4.1.8. The entry is not a record, so §10.2.4 never reaches it; this condition\n   is the only thing that makes an unimplemented claim block, which is what §4.1.3\n   relies on."}
{"id": "CR-1#c110", "text": "No record of the current round remains in `draft` or `queued`.", "source": "description", "span": "4. No record of the current round remains in `draft` or `queued`."}
